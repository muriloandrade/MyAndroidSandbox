<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- CustomActionBar Activity Theme -->
    <style name="CustomActionBarTheme" parent="android:Theme.Holo.Light">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:actionMenuTextColor">#0F5</item>
    </style>

    <style name="MyActionBarStyle"
        parent="Widget.AppCompat.Light.ActionBar.Solid">
        <item name="android:titleTextStyle">@style/MyActionBarTitleTextStyle</item>
        <item name="android:background">@drawable/action_background</item>
    </style>

    <style name="MyActionBarTitleTextStyle"
        parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#009</item>
    </style>

</resources>
